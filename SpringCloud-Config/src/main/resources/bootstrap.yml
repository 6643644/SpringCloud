server:
  port: 8990
  
#取得其他service設定檔案(從遠端的git)
spring:
  cloud:
    config:
      server:
        git:
          uri: C:\doc\.git
#          uri: https://github.com/6643644/doc.git
#          以上URI 可以直接下載.git路徑的直接使用
#          username: XXXX
#          password: XXXX
          search-paths: test-spring-cloud-config
          default-label: master
          
          
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8994/eureka/